.user-create-container
  - if @success.present?
    .success
      %p
        = @success
  - else
    .error
      %p
        = @errors
    .create-button
      = link_to "Create User", new_user_path
